[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "corex-agent-runtime"
version = "0.0.0"
description = "Corex AgentOS Python execution plane"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/corex_runtime"]
